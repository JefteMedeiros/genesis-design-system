import { Meta } from '@storybook/addon-docs'
import { bodyTokens, headingTokens } from "../../tokens/fontSizes"
import { TokensGrid } from "../../partials/TokensGrid"

<Meta title="Tokens/Typography" />

# Typography

Those are the Typography variations of Genesis UI.

## Headings
<TokensGrid hasRemValue={true} tokens={headingTokens} />
## Body
<TokensGrid hasRemValue={true} tokens={bodyTokens} />